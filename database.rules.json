{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
        "id": {
          ".write": "auth != null && (auth.uid === $uid || auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
        },
        "name": {
          ".write": "auth != null && (auth.uid === $uid || auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
        },
        "email": {
          ".write": "auth != null && (auth.uid === $uid || auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
        },
        "phone": {
          ".write": "auth != null && (auth.uid === $uid || auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
        },
        "photoUrl": {
          ".write": "auth != null && (auth.uid === $uid || auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
        },
        "authProvider": {
          ".write": "auth != null && (auth.uid === $uid || auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
        },
        "joinedDate": {
          ".write": "auth != null && (auth.uid === $uid || auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
        },
        "createdAt": {
          ".write": "auth != null && (auth.uid === $uid || auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
        },
        "role": {
          ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
        },
        "tickets": {
          ".read": "auth != null && (auth.uid === $uid || auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
          ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
        },
        "bookings": {
          ".read": "auth != null && (auth.uid === $uid || auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
          ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
        }
      }
    },
    "staff": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')",
        ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')",
        ".validate": "newData.hasChildren(['email', 'role']) && (newData.child('role').val() === 'super_admin' || newData.child('role').val() === 'event_manager' || newData.child('role').val() === 'counter_staff' || newData.child('role').val() === 'auditor' || newData.child('role').val() === 'admin' || newData.child('role').val() === 'ticket_counter')"
      }
    },
    "events": {
      ".read": true,
      ".write": false,
      "$eventId": {
        ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || auth.token.role === 'event_manager')"
      }
    },
    "seats": {
      ".read": true,
      ".write": false,
      "$eventId": {
        ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || auth.token.role === 'event_manager')"
      }
    },
    "coupons": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || auth.token.role === 'event_manager' || root.child('staff').child(auth.uid).exists())",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || auth.token.role === 'event_manager')"
    },
    "orders": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists() || data.child('userId').val() === auth.uid)",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')",
      "$orderId": {
        ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists() || data.child('userId').val() === auth.uid)"
      }
    },
    "passes": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
    },
    "tickets": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')",
      "$ticketId": {
        ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists() || data.child('ownerId').val() === auth.uid)"
      }
    },
    "bookings": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')",
      "$bookingId": {
        ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists() || data.child('userId').val() === auth.uid)"
      }
    },
    "reservations": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists() || data.child('ownerId').val() === auth.uid)",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists() || data.child('ownerId').val() === auth.uid)"
    },
    "reservation_owners": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists() || data.child('ownerId').val() === auth.uid)",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())"
    },
    "reservation_events": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
    },
    "pending_orders": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists() || data.child('userId').val() === auth.uid)",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists() || data.child('userId').val() === auth.uid)"
    },
    "processed_orders": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists() || data.child('userId').val() === auth.uid)",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())"
    },
    "organizers": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || auth.token.role === 'event_manager')",
      "$organizerId": {
        ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists() || data.child('userId').val() === auth.uid)"
      }
    },
    "reviews": {
      ".read": true,
      "$reviewId": {
        ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).child('role').val() === 'admin' || (!data.exists() && newData.child('userId').val() === auth.uid))",
        ".validate": "!newData.exists() || (newData.hasChildren(['id','eventId','userId','userName','rating','comment','createdAt','status','isVerifiedBuyer']) && newData.child('rating').isNumber() && newData.child('rating').val() >= 1 && newData.child('rating').val() <= 5 && newData.child('comment').isString() && newData.child('comment').val().length <= 2000)"
      }
    },
    "favorites": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
    },
    "staff_uploads": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
    },
    "counters": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
    },
    "counter_shifts": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
    },
    "sales": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
    },
    "app_config": {
      ".read": false,
      ".write": false,
      "merchant_upi": {
        ".read": true,
        ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
      }
    },
    "audit_log": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
    },
    "notifications": {
      ".read": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin' || root.child('staff').child(auth.uid).exists())",
      ".write": "auth != null && (auth.token.admin === true || auth.token.role === 'admin' || auth.token.role === 'super_admin')"
    }
  }
}
